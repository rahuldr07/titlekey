node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
.claude/

# The untouched ZIP extraction and the ZIP itself hold the unredacted demo
# data (identity-document-shaped values). This repository is public, so they
# stay local-only. title-crm-897/ is the REDACTED reference copy and IS committed.
original-reference/
Title CRM*-handoff.zip
